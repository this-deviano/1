# TASKS — append-only (status: TODO / DOING / DONE / BLOCKED / DROPPED + ADR)

- [ ] TASK-000 M1: projection audit (Refs: SB-002 §3, P-05) — STATUS: DONE
- [ ] TASK-001 M1: constitutional bootstrap (Refs: Genesis §22.2, §31.4) — STATUS: DONE
- [ ] TASK-002 M1: first-light 12-item checklist (Refs: §32, AG-02) — STATUS: DONE
- [ ] TASK-003 M1: ADR-0001 + ADR-0002 committed verbatim + INDEX (Refs: Genesis §26.3) — STATUS: DONE
- [ ] TASK-004 M1: fix first-light FAILs — FL-06 seed PRNG, FL-07 meter gradient, FL-08 local fonts, FL-09 confirm→non-modal (Refs: AG-01, C-07) — STATUS: DONE
- [ ] TASK-005 M1: OPFS persistence migration (Refs: P-20, ADR-0002 §3; prereq TASK-007) — STATUS: DONE (feat/opfs-persistence 8cd4e0e: atomic song.json temp+verify+swap, manifest, read-only legacy migration, usage readout in LR-0001; known limits logged in WORKLOG). BOX 6 CLOSED by TASK-007: media/<sha>.wav content-addressed store + manifest media index.
- [ ] TASK-006 M1: undo past reload — persist history with autosave (Refs: P-06) — STATUS: DONE (8cd4e0e + fix/undo-conformance cca4170 under R-1: snapshots are §11.6 Time Machine, cap 100; in-session stack is §10.2, cap 10,000/512 MB; restore PUSHES an entry. Harness-verified: cap enforced at 10,000 (10,350 ops → depth 10,000), restore 300 → 301, snapshot store exactly 100.)
- [ ] TASK-007 M1: microphone recording slice — getUserMedia pro defaults, worklet ring buffer, LR-#### permission panel, monitor default off (Refs: P-02, P-14) — STATUS: DONE (4 of 4 boxes, fix/feat stack 4221d07/67e40a9, HARNESS-VERIFIED): getUserMedia with echoCancellation/noiseSuppression/autoGainControl all false; AudioWorklet 60 s ring buffer; 4-beat count-in, stop → take → clip on the armed audio track; count-in TRIMMED from the take; media content-addressed to OPFS media/<sha256> (verified sha256(read-back) == clip pointer, mono 32-bit float, peak > 0, non-zero samples > 0, duration 0.752 s ≈ expected count-in-excluded); LR-0007 panel on denial (real headless denial: NotFoundError) with retry, zero page errors; monitoring default OFF (asserted) + headphone warning on enable; both-theme record-armed screenshots captured.
- [ ] TASK-008 M1: render determinism — E-28 double-export checksum, documented scope (Refs: E-28, P-08) — STATUS: BLOCKED (HV-5 FAILED at runtime, SB-004 harness: five offline renders of one unchanged Song produced five distinct SHA-256 values, pairwise max|Δ| 4.17e-7…4.77e-7 ≈ −127 dBFS; `sameAsFirst=[true,false,false,false]` rules out a warm-up artifact. Our side is deterministic by construction (per-render seed reset, stable event ordering, one graph builder) and the independently-produced parity residual is the same order → platform float noise, not a scheduling divergence. AMM-003-candidate filed per AG-10. `ok` stays BIT-EXACT; not widened. BLOCKED on a maintainer ruling, not on code.)
- [ ] TASK-008 M1: render determinism — E-28 double-export checksum, documented scope (Refs: E-28, P-08) — STATUS: DOING (duplicate historical row; superseded by the BLOCKED row above)
- [ ] TASK-009 M1: command inventory table — id | pointer | key | palette; fix gaps (Refs: P-11) — STATUS: DONE
- [ ] TASK-010 M1: P-02 rehearsal — fresh-state reset definition + timed journey (Refs: P-02) — STATUS: DONE (reset defined; timing now automated as P-02-PROXY = 3,875 ms)
- [ ] TASK-011 M1: engine audit orphan ORPH-A-1 — p.gain never reaches audio path (Refs: TASK-000 audit) — STATUS: DONE (fix/orphan-gain 9cc0980: placement.gain × velocity × track.gain in fireVoice)
- [ ] TASK-012 M1: engine audit orphan ORPH-A-2 — placement.mute ignored in renderWav (Refs: TASK-000 audit) — STATUS: DONE (AUDIT CORRECTION: p.mute was ALREADY honored in renderWav — observed at 4a18447:514, 7d1755a:517, tip; no defect existed. Covered structurally by the unified materialEvents mapping + parity guard)
- [ ] TASK-013 M1: engine audit orphan ORPH-B-1 — metronome/count-in constants unpersisted (Refs: TASK-000 audit; needs song+model field) — STATUS: DONE (fix/state-cycle b26410e: cycle → Song schema field, metronome → persisted pref luthier.metronome.v1; AMM-001-candidate filed per AG-05; LR-0005 registered)
- [ ] TASK-014 M1: engine audit orphan ORPH-D-1 — renderWav omits cycle material (Refs: TASK-000 audit; parity with scheduleRange) — STATUS: DONE (fix/orphan-render-parity d8b053c: materialEvents is the single Song→event map; both schedulers consume it)
- [ ] TASK-015 M1: engine audit orphan ORPH-D-2 — offline render drops DynamicsCompressor attack/release constants (Refs: TASK-000 audit) — STATUS: DONE (d8b053c) + CORRECTION (SB-004 ee0667c): the fix was INCOMPLETE — renderBuffer() kept only `{ comp }` from buildMasterGraph() and connected it straight to the destination, so the offline bus never applied MASTER_GAIN. Exports were ≈0.92 dB hotter than the live bus (P-15). The parity guard's live leg had copied the same wiring, so the two legs agreed with each other and both disagreed with production. Both now run comp → master(MASTER_GAIN) → destination. Found by the TASK-021 harness — the static half could not have seen it.
- [ ] TASK-016 M1: worklet underrun counter wiring — status.underruns never increments (Refs: E-21, P-07; blocked: no runtime/AudioWorklet verification possible) — STATUS: TODO, was BLOCKED (the TASK-021 harness now makes this verifiable: drive a long pass and read app.engine().underruns. No longer blocked on runtime — blocked on nobody having done it.)
- [ ] TASK-016 M1: remote sync & verification — push main (E-001 doc-class) + fix stack per E-002; verify origin state (Refs: SB-003 §2, P-20 spirit) — STATUS: DONE (SB-003: main 4a18447→ff21132 + 9 branches. SB-004 TASK-020 re-verified: origin/main STILL ff21132, stack still linear, all 9 branch SHAs match the briefing — NO LOSS.)
- [ ] TASK-017 M1: xrun/taskset real-time discipline gates (Refs: ADR-0001; activates at Tauri shell) — STATUS: DROPPED + ADR-0001 (preview layer inherits spirit, not letter)
- [ ] TASK-017 M1: in-app E-28 determinism self-test — app.selftest.determinism() renders twice in-memory, SHA-256 compare, inline PASS/FAIL (Refs: SB-003 §5; converts HV-5 to one click) — STATUS: DONE (shipped; its RUNTIME verdict is FAIL — see TASK-008)
- [ ] TASK-018 M1: wrap coach localStorage key write in visible failure handling (Refs: P-14; minor, found in FL-11) — STATUS: TODO (capacity tail)
- [ ] TASK-019 M1: command-coverage gaps from inventory: keys for Load/Export, pointer affordances for Undo/Redo/snap, Del binding for placement delete, palette rows for duplicate/delete placement (Refs: P-11) — STATUS: TODO (capacity tail)
- [ ] TASK-020 M1: parity guard — app.selftest.renderparity() renders reference song via BOTH schedulers (Refs: SB-003 §4, P-14) — STATUS: DONE + CORRECTED (015b734 under R-2: dual-number verdict — measured max|Δ|, ship gate −80 dBFS, constitution floor −96 dBFS, gateMet/floorMet separately). MEASURED by harness: −126.4 dBFS → **floorMet TRUE**; R-2's assumed −80 dBFS divergence does not exist above the platform noise floor, so TASK-023 has no convergence work left.
- [ ] TASK-021 M1: headless harness + verification battery (Refs: E-004) — STATUS: DONE (48521fb: tests/preview/hv.spec.ts, playwright.config.ts, window.app seam in src/lib/devapi.ts, one command `bun run test:preview`, evidence in docs/evidence/sb004/). **9 of 10 checks PASS, 1 FAIL (determinism/HV-5, real — see TASK-008).** Covers HV-1 console-clean (0 errors/warnings over 10 s), HV-2 AudioContext running, HV-4 propagation (2.9 ms vs ≤8 ms budget), parity, determinism + 4-render diagnostic, HV-6 both themes, P-02-PROXY (3,875 ms), undo/storage/cap, TASK-007 denial + fake-tone + armed screenshots.
- [ ] TASK-022 M1: undo conformance per R-1 — STATUS: DONE (cca4170, 4 of 4 boxes): (a) in-session cap 10,000 entries OR 512 MB estimated serialized, FIFO eviction — harness-verified by driving 10,350 mutations to depth exactly 10,000 (50 MB, byte cap not reached); (b) cross-reload = §11.6 Time Machine, snapshot-granular — ratified, documented; (c) restore PUSHES an undoable entry, never clears — Load and cmdRestoreSnapshot() both push (harness: 300 → 301); (d) AMM-002-candidate filed + docs/spec/schema-changelog.md records the additive seed/cycle/media fields.
- [ ] TASK-023 M1: parity gate per R-2 — STATUS: DONE (015b734, dual-threshold display shipped in the console line and the LR-0006 panel; the self-test always prints the measured value and BOTH thresholds). Convergence: **no work remains** — the measured residual (−126.4 dBFS) is already 30 dB below the −96 dBFS constitution floor and is the same order as Chromium's own per-render noise. R-2's premise is superseded by AMM-003-candidate.
- [ ] TASK-024 M1: confirm audit — enumerate every confirm/two-step guard; reversible → delete, irreversible → keep (P-06) — STATUS: DONE (67612c9). Enumeration: exactly ONE guard exists in the app (New Song). Zero confirms deleted, because there were none on reversible actions — every reversible mutation already relies on undo alone. The one guard is on the one IRREVERSIBLE action (it deletes song.json, the manifest and the §11.6 history) and is retained; it was moved from the Rail button into cmdNewSong() so the command palette's "New Song (reloads)" row can no longer bypass it and destroy work on a single click. Esc disarms.
- [ ] TASK-025 M1: M1 exit report — STATUS: DONE (this session's SESSION REPORT SB-004 + WORKLOG §SB-004). MILESTONE deliberately NOT advanced: see the entry below.
- [ ] TASK-026 M1: root-cause the offline-render nondeterminism (Refs: AMM-003-candidate, TASK-008) — STATUS: TODO. Bisect a minimal graph in headless Chromium (source → gain → destination, then +biquad, then +compressor) to isolate whether the ~4e-7 per-render divergence originates in a specific node. Needed before AMM-003 can be ruled on with confidence rather than inference.
- [ ] TASK-027 M1: meter scale — the maintainer's SB-004 eyeball check expected notches at 0/−3/−6/−12/−18/−24/−36/−48; the shipped meter has TWO zone notches (78% amber, 90% signal) on a LINEAR-amplitude fill, i.e. no dB scale at all (Refs: G-12 FL-07 fix vs the intended scale) — STATUS: TODO. Decide whether the design wants a dB-referenced scale (P-07 honest numbers argues yes) or the plain amplitude bar; the FL-07 gradient fix is correct either way.
- [ ] TASK-028 M1: surface host renders ONE surface at a time (Refs: HV-4 premise) — STATUS: TODO. HV-4 asks the maintainer to observe the Loom miniature and Ivory updating "in the same frame" as a Lattice edit; with a tabbed single-surface host there is no such DOM. Either the runbook premise changes (serial cross-surface checks, as the harness now does) or the product wants a simultaneous multi-surface mode. Filed as a maintainer design question, not a defect.
